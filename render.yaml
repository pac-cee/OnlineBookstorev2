# render.yaml

services:
  - type: web
    name: onlinebookstore-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: starter      # free tier
    buildCommand: ""   # Dockerfile handles build
    startCommand: ""   # Dockerfile’s ENTRYPOINT starts MySQL + Apache

# (No "databases:" section needed, since MySQL is bundled in the Dockerfile
# and setup.sql creates the 'bookapp' schema on first run.)
